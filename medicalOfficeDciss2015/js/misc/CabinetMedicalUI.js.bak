
// récupérer le template et le stocker dans la variable template
var template = require("./cabinetMedical.html");

// récupérer le CSS et l'agréger selon la règle du GulpFile
require("./cabinetMedical.css");

/**
 * Attent un module angular, puis créé un composant et l'ajoute
 * @param {type} mod
 * @returns {undefined}
 */
module.exports = function (mod) {

    // créer un service
    var proxyNF = require("./proxyNF.js")(mod);

    // code javascript éxécuté à la création d'une balise
    var controller = function (proxyNF) {
        // Cette fonction sera appelée à la manière d'un constructeur pour instancier un element CabinetMedical
        var label = "label";
        // appel dans le code html du template : {{$ctrl.label}}
    };
    
    // code équivalent au code ci-dessus, a condition de modifier la case de l'appel
    function Controller(proxyNF){
        // Cette fonction sera appelée à la manière d'un constructeur pour instancier un element CabinetMedical
    }

    // les arguments nécéssaires à l'instanciation du controlleur ( les arguments de la fonction controller() )
    controller.$inject = [proxyNF];

    // enregistrer un nouveau composant de nom cabinetMedical(camel case obligatoire)
    mod.component("cabinetMedical", {
        template: template,
        bindings: {
            titre: "@"
            // appel dans le code html du template : {{$ctrl.titre}}
        },
        controller: controller
    });
};

